# MongoDB Connection
# The URI below assumes the mongo_db container is reachable at localhost:5001 or via the service name 'mongo_db' in a Docker environment.
MONGODB_URI="mongodb://appuser:dbuser123@localhost:5001/myapp?authSource=admin"

# Security
JWT_SECRET="change_this_secret_in_production"

# Server Port
PORT=3001
